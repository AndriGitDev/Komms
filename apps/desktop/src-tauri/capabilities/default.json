{
  "$schema": "gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window talks to the node backend over IPC and listens for node events. No plugins, no shell/file-system/network capabilities — the webview reaches the outside world only through the audited command surface.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
